<form method="post" action="{{ path('app_resume_submission_delete', {'id': resume_submission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resume_submission.id) }}">
    <button class="btn">Delete</button>
</form>
